import { ReactElement } from "react";
import { Home, MainText, UserList } from "@/components";

const Page = (): ReactElement => (
  <>
    <Home />
    <MainText />
    <UserList />
  </>
);

export default Page;
